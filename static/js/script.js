document.addEventListener('DOMContentLoaded', () => {
  // Small UI enhancement: focus the input
  const input = document.querySelector('.task-input');
  if (input) input.focus();

  console.log('To-Do App loaded');
});
